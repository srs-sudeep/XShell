# Core dependencies
flask>=2.3.3
flask-socketio>=5.3.6
python-engineio>=4.8.0
python-socketio>=5.10.0
eventlet>=0.33.3

# Shell engine
prompt_toolkit>=3.0.43
Pygments>=2.17.2

# Beautiful output
rich>=13.7.0

# System info (optional but recommended)
psutil>=5.9.8

# Build / packaging
pyinstaller>=6.3.0

# Optional: Windows desktop notifications (notify plugin)
# win10toast>=0.9  — install manually if needed on Windows

# Optional: LLM plugin keys loaded via envload
# (No extra deps — uses stdlib urllib only)
